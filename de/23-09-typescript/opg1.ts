let num1:number = Number(prompt('a number'));
let num2:number = Number(prompt('a number'));

function addTwo (n1: number, n2:number): number {
    return n1 + n2;
}

console.log(addTwo(num1, num2));

